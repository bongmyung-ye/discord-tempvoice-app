# Interaction Flow

TempVoice 패널은 사용자가 직접 음성 채널을 관리할 수 있도록 버튼 기반 인터페이스를 제공한다.

1. 관리자가 `/tempvoice-panel` 명령어를 실행한다.
2. 봇이 TempVoice 관리 임베드와 버튼 패널을 전송한다.
3. 사용자가 버튼을 누르면 custom id를 기준으로 액션을 분기한다.
4. 채널 소유자 여부와 현재 음성 채널 상태를 검증한다.
5. 필요한 경우 모달 또는 셀렉트 메뉴를 표시한다.
6. 검증된 액션만 Discord 채널 권한과 상태에 반영한다.
